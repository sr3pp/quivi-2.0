export default defineEventHandler((event: any) => {
  //const cookie = getCookie(event, "auth");
  //const { pathname } = getRequestURL(event);
  //console.log("the cookie in server", cookie);
  //console.log("the route in server", pathname);
});
